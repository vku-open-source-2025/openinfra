PROJECT_NAME="Infrastructure Management System"
MONGODB_URL=mongodb://mongo:27017
DATABASE_NAME=gis_db
ADMIN_JWT_SECRET=change-me
ADMIN_JWT_ALGORITHM=HS256
ADMIN_JWT_EXPIRE_MINUTES=1440
ADMIN_DEFAULT_USERNAME=admin
ADMIN_DEFAULT_PASSWORD=admin123

# Cloudflare Turnstile (Captcha for public forms)
TURNSTILE_SECRET_KEY=your-turnstile-secret-key-here
TURNSTILE_SITE_KEY=your-turnstile-site-key-here

# AI Agent - Google Gemini API
# Get your API key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
GEMINI_TEXT_MODEL=text-embedding-004
GEMINI_VISION_MODEL=gemini-2.5-flash

# Duplicate Detection Configuration
# Similarity threshold (0.0-1.0) - incidents above this score are considered duplicates
DUPLICATE_SIMILARITY_THRESHOLD=0.85
# Weight for text similarity vs image similarity (0.0-1.0)
DUPLICATE_TEXT_WEIGHT=0.6
DUPLICATE_IMAGE_WEIGHT=0.4
# Time window in hours to search for duplicates (default: 168 = 7 days)
DUPLICATE_TIME_WINDOW_HOURS=168
# Location radius in meters to search for duplicates (default: 50 meters)
DUPLICATE_LOCATION_RADIUS_METERS=50.0
# Maximum number of images per incident to analyze for similarity
GEMINI_MAX_IMAGES_PER_INCIDENT=5

# Gemini Chat/Agent Model Configuration
# Set to true to use live preview model for real-time streaming (gemini-2.5-flash-live-preview)
# Set to false to use stable model for production (gemini-2.5-flash)
# Default: false (uses stable model)
GEMINI_CHAT_MODEL_USE_LIVE=false
# Stable model (production-ready, recommended)
GEMINI_CHAT_MODEL_STABLE=gemini-2.5-flash
# Live API model (for WebSocket streaming - bidiGenerateContent)
GEMINI_CHAT_MODEL_LIVE=gemini-2.0-flash

# Note: gemini-2.5-flash does NOT support Live API
# For Live API (WebSocket streaming), use gemini-2.0-flash
# For regular streaming (generateContentStream), use gemini-2.5-flash

# Optional: To use gemini-live-2.5-flash-preview instead of gemini-2.0-flash for Live API
# Note: This is a half-cascade model that will be deprecated soon
# Uncomment and modify GEMINI_CHAT_MODEL_LIVE if you want to use it:
# GEMINI_CHAT_MODEL_LIVE=gemini-live-2.5-flash-preview
